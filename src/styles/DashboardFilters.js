export const styles = theme => ({
  sortBy: {
    marginLeft: '1em'
  },
  yearSelector: {
    textAlign: 'right',
    marginTop: '0',
    [theme.breakpoints.up('sm')]: {
      marginTop: '-0.5em'
    }
  }
});
